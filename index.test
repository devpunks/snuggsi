const
  { test }
    = require ('test')

, { Test, Server }
    = require ('.')


test ('snuggsi.Test',
  t => { t.assert (Test), t.end () })


test ('snuggsi.Server',
  t => { t.assert (Server), t.end () })
