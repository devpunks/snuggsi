const
  { test } = require ('.')

//test2 `.test`
//  (t => { t.skip `when loading tape causes tests to run`, t.end () })

test `legacy test`
  ( true === true )

test `snuggsi.test HOT DAMN!`
  ( 'foo' === 'foo' )

test `.fetch`
  ( 'fetch' in require ('.') )

test `.Server`
  ( 'Server' in require ('.') )

test `.Resource`
  ( 'Resource' in require ('.') )

test `Single Assertions`
  ( assert => assert ('foo' === 'foo'))

test `Multiple Assertions`
( assert => assert
    ('foo' === 'foo')
    ('bar' === 'bar')

  && assert
    ('baz' === 'baz')
)
