#!/usr/bin/env bash

set -u

declare -r \
  TEST='node --test' \
  SUITE="${@:-$( echo  .{/*,}/*.test{.js,.es} )}"
# TODO add .test suite extensions
# SUITE="${@:-'.{/**,}/*.test{,.js,.es}'}"


echo
echo "🚨  Testing, Testing, 1,2,3, ..."
echo
echo "  Suite 👉  $SUITE"
echo

$TEST $SUITE
