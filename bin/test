#!/usr/bin/env bash

set -u

function bin () {
  echo ${SNUGGSI}/node_modules/.bin
}

declare -r \
  TEST='node --test' \
  SUITE=".{/**,}/*.test{,.js,.es}"


echo
echo "🚨  Testing, Testing, 1,2,3, ..."
echo
echo "  Suite 👉  $SUITE"
echo

$TEST $SUITE
