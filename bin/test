#!/usr/bin/env bash

declare -rx NODE_PATH=$SNUGGSI
declare -rx PORT=${PORT:-1234}
declare -r  BIN="$SNUGGSI/node_modules/.bin"
declare -r  SPEC="$BIN/tap-spec"
declare -r  TEST="$BIN/tape --require $SNUGGSI"
declare -r  SUITE="index.test"


echo
echo "🚨  Testing, Testing, 1,2,3, ..."
echo
echo "  Entry 👉  $ENTRY"
echo "  Suite 👉  $TESTS"
echo "  Snuggsi ツ $SNUGGSI"
echo "  Bin ツ $BIN"
echo


$TEST $TESTS | $SPECS
