#!/usr/bin/env bash

# Thanks Tom M. (@tmornini) for ./crank

# && bin/cover      \
bin/compile         \
  && bin/lint       \
  && bin/weigh      \
  && bin/distribute \
  && bin/version    \
  && bin/publish    \
  && bin/deploy
