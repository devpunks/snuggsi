#!/bin/bash

COUNT="wc -l" # Output lines
TRIM=" tr -d" #Translate deletion
DIST=./dist/snuggsi.es # Distribution
LINT="./node_modules/.bin/standard $DIST"
NUMBER_OF_WARNINGS=`$LINT | $COUNT | $TRIM " "`


echo
echo "⚠️  StandardJS Warnings 👉  $NUMBER_OF_WARNINGS"
echo
