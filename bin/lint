#!/bin/bash

COUNT="wc -l"
DIST=./dist/snuggsi.es
LINT=./node_modules/.bin/standard
COMMAND="$LINT $DIST"
NUMBER_OF_WARNINGS=`$COMMAND | $COUNT | tr -d " "`


echo
echo "⚠️  StandardJS Warnings 👉  $NUMBER_OF_WARNINGS"
echo
