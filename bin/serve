#!/usr/bin/env node

const
  root =
    require ('path')
      .resolve (__dirname, '..')

console.warn (path)
// --expose-http2 \ # Experimental HTTP/2 flag https://nodejs.org/api/cli.html#cli_expose_http2
