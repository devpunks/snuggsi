#!/usr/bin/env bash

export NODE_PATH=`pwd`
export PORT=${PORT:-3000}
export ROOT=${1:-`pwd`}

echo NODE PATH $NODE_PATH
echo PORT $PORT
echo ROOT $ROOT

SERVE=bin/serve.es


node           \
  --require ./ \
$SERVE
