#!/usr/bin/env bash

declare -x NODE_PATH=`pwd`
declare -x PORT=${PORT:-3000}
declare -x DIRECTORY=${1:-`pwd`} # default working directory to current location

node           \
  --require $SNUGGSI \
$SNUGGSI/bin/serve.es
