#!/usr/bin/env bash

declare -rx DIRECTORY=${1:-$(pwd)}
declare -r  SNUGGSI=${SNUGGSI:-$( realpath '.' )}
declare -r  WATCHER=$SNUGGSI/watch/index.es

echo "$SNUGGSI"
echo " $DIRECTORY 👀  Watching Resources for changes..."


node $WATCHER
