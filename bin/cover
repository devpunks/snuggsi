#!/bin/bash

export THRESHOLD=95

echo
echo "Coverage threshold set to 👉  $THRESHOLD"
echo

# ./node_modules/.bin/nyc \
#   --extension=.es       \
#   --reporter=text       \
#   bin/test

./node_modules/.bin/nyc         \
  check-coverage                \
    --lines        $THRESHOLD   \
    --function     $THRESHOLD   \
    --statements   $THRESHOLD   \
  bin/test
