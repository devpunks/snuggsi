const {
  test, describe,
  beforeEach: before, afterEach: after
} = require('node:test')
, assert = require('assert')
, { skip, todo } = test.it

const { debounce, throttle, limit }
  = require('./govern.js')


todo( 'TODO: debounce' )
todo( 'TODO: throttle' )
skip( 'SKIP: limit' )
