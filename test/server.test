const { test, browse, serve }
  = require ('test')

const
  options = { dist: 'dist' }
, Server  = serve (options)


test ('new Server', t => {

  t.doesNotThrow
    ( _ => new Server )

  t.end ()
})

