const { describe, browse }
  = require ('test')

const
  options = { dist: 'dist' }
, Server  = serve (options)


describe ('new Server', test => {

  test.doesNotThrow
    ( _ => new Server (options) )

  test.end ()
})

