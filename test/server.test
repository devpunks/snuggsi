const
  { describe, browse, serve }
    = require ('test')

const
  options = { dist: 'dist' }
, Server  = new (require ('./server'))(options)

describe ('new Server', test => {

  test.doesNotThrow
    ( _ => new Server (options) )

  test.end ()
})

