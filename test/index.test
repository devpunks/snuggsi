const
  { test, serve, browse }
    = require ('.')


test ('describe',
  t => { t.assert (test), t.end () } )

test ('serve',
  t => { t.assert (serve), t.end () } )

test ('browse',
  t => { t.assert (browse), t.end () } )
