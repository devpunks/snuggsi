const
  { test, serve, browse, fetch, read, get }
    = require ('.')


test ('fetch',
  t => { t.assert (fetch), t.end () } )

test ('read',
  t => { t.assert (read), t.end () } )

test ('serve',
  t => { t.assert (serve), t.end () } )

test ('browse',
  t => { t.assert (browse), t.end () } )

test ('get',
  t => { t.assert (get), t.end () } )

test ('test',
  t => { t.assert (test), t.end () } )
