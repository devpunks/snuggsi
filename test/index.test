const
  { test, serve, browse, request }
    = require ('.')


test ('test',
  t => { t.assert (test), t.end () } )

test ('serve',
  t => { t.assert (serve), t.end () } )

test ('browse',
  t => { t.assert (browse), t.end () } )

test ('request',
  t => { t.assert (request), t.end () } )
