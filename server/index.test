const
  { test, browse }
    = require ('test')

, Server
    = require ('.')


test ('new Server does not throw', t => {
  var s

  t.doesNotThrow ( _ => (s = new Server) )

  t.end ()
})


test ('serve defaults to `public` when called with template literal', t => {

  (new Server).serve ``

  t.end ()
})
