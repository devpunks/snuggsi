const
  { test, browse }
    = require ('test')

let
  options = { dist: 'dist' }
, Server  = require ('.')


test ('new Server', t => {

  t.doesNotThrow
    ( _ => new Server (options) )

  t.end ()
})
