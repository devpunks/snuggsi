const
  { test, browse }
    = require ('test')

, Server
    = require ('.')


test ('new Server', t => {
  var s

  t.doesNotThrow ( _ => (s = new Server) )

  t.end ()
})

test ('.shutdown', t => {

  t.end ()
})
