const
  {test, browse}
    = require ('test')

, document = browse `index`


test ('new Element', t => {
  const document = browse (interface)

  test.ok
    (document.querySelector `foo-bar`)

  test.end ()
})
