const
  {test, browse}
    = require ('test')

, document = browse `index`


test ('new Element', t => {
  console.warn (document.body.outerHTML)

  t.ok (document.querySelector `foo-bar`)

  t.end ()
})
