const
  interface = 'index'
, {describe, test, browse} = require ('../test/index.es')

describe ('new Element', test => {
  const document = browse (interface)

  console.warn (arguments)
//test.ok (document.querySelector `foo-bar`)

//test.end ()
})
