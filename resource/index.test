const
  { test, read, fetch }
    = require ('test')

, { Server, Resource }
    = require ('..')

, route
    = require ('../middleware')
      .route  ( '/foo', Resource `server/fixtures` )


test.only ('new Resource', t => {

  const
    stack = [ route ]
    server =
      (new Server)
      .serve ``

  console.warn (Resource `server/fixtures`({ method: 'PATCH' }))

  server.close ``
  t.end ()
})
