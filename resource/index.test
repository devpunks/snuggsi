const
  path = '/foo/'
, URL  = `http://localhost:8181${path}`

, { test, read, fetch }
    = require ('test')

, { Server, Resource }
    = require ('..')

, route
    = require ('../middleware')
      .route  ( `${path}{id}`, Resource `server/fixtures` )


test.only ('new Resource', async t => {

  const
    method = 'OPTIONS'

  , endpoint =
      [ `${URL}14`, { method } ]

    server =
      (new Server ( [ route ] ))
      .serve ``

  , response =
      await fetch ( ... endpoint )

  , { status, body } = response

  console.warn ('Resp', status, response.headers,  await response.text ())


  server.close ``
  t.end ()
})
