const
  { test, read, fetch }
    = require ('test')

, { Server, Resource }
    = require ('..')

, { route }
    = require ('../middleware')

test.only ('new Resource', t => {

  console.warn (Resource `server/fixtures`({ method: 'PATCH' }))

  t.end ()
})
