const
  { test, read, fetch }
    = require ('test')

, { Server }
    = require ('..')

void function () {

  var snuggs = 'damn'

  require ('..')
} ()

test.only ('new Server does not throw', t => {
  t.doesNotThrow ( _ => new Server )

  t.end ()
})
