const
  { test, serve }
    = require ('test')

const
  puppeteer
    = require ('puppeteer')

, browser
    = await puppeteer.launch ()


test ('new HTMLElement', t => {

  const
    server = serve ``


  console.warn (browser)

  server.close ``
  t.end ()
})

