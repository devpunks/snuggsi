const
  { test, fetch }
    = require ('test')

, { Server }
    = require ('..')

test.only ('route URI to middleware', async t => {

  const
    server
      = (new Server).serve ``

  await fetch ('http://localhost:8181')


  t.ok (true)

  server.close ()
  t.end ()
})
