const
  { test, fetch }
    = require ('test')

test.only ('assets', async t => {

  const
    server
      = (new Server).serve ``

  await fetch ('http://localhost:8181')


  t.ok (true)

  server.close ()
  t.end ()
})
