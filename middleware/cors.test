const
  { test, browse, get }
    = require ('test')

, assets = require ('.')

console.log (Object.keys (assets))

, Server
    = require ('server')

, request
   = require ('request')

test ('Access-Control-Allow-Origin: *', t => {

  const
    server
      = (new Server).serve ``

  request.get ('http://localhost:8181', response => {
    console.log (response.headers)
  })
})
