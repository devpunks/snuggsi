const
  { test, fetch }
    = require ('test')

, { assets }
    = require ('.')

, { Server }
    = require ('..')


test ('random location of assets')

test.only ('assets', async t => {

  const
    server
      = (new Server).serve ``

  await fetch ('http://localhost:8181')


  t.ok (true)

  server.close ()
  t.end ()
})
