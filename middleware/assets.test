const
  { test, browse, get }
    = require ('test')

, { assets }
    = require ('.')

, Server = require ('server')


test ('assets', t => {

  var server = (new Server).serve ``

  server.close ()
  t.end ()
})
