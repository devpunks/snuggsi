const
  { test, browse, get }
    = require ('test')

, { assets }
    = require ('.')

, Server
    = require ('server')

, request
   = require ('request')


test ('random location of assets')

test ('assets', t => {

  const
    server
      = (new Server).serve ``

  console.warn ( 'function' == server)


  t.ok (true)

  server.close ()
  t.end ()
})
