const
  { test, browse }
    = require ('test')

, { assets }
    = require ('.')

, { Server }
    = require ('..')


test.only ('random location of assets')

test ('assets', async t => {

  const
    server
      = (new Server).serve ``

  await fetch ('http://localhost:8181')


  t.ok (true)

  server.close ()
  t.end ()
})
