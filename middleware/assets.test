const
  { test, browse, get }
    = require ('test')

, { assets }
    = require ('.')

, Server
    = require ('server')


test ('assets', t => {

  const
    server
      = (new Server).serve ``

  t.ok (true)

  server.close ()
  t.end ()
})
