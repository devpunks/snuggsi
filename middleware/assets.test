const
  { test, browse, get }
    = require ('test')

, { assets }
    = require ('.')

, Server
    = require ('server')

, request
   = require ('request')


test ('random location of assets')

test ('assets', t => {

  const
    server
      = (new Server).serve ``

  request
    .get ('http://localhost:8181', response => { })


  t.ok (true)

  server.close ()
  t.end ()
})
