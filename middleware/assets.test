const
  { test, browse, get }
    = require ('test')

, { assets }
    = require ('.')

, Server
    = require ('server')


test ('assets', t => {

  const
    server
      = (new Server).serve ``

  console.warn (server)


  t.ok (true)

  server.close ()
  t.end ()
})
