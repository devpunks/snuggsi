const
  { test, serve, read, fetch }
    = require ('test')


test.only ('Resource HTML GET / Accept: text/html', async t => {

  const
    accept  = 'text/html'
  , URI     = `http://localhost:8181/`
  , server  = serve `server/fixtures`
  , dist    = read  `resource/fixtures/index.html`
  , options = {
      compress : false
    , headers  : { accept } }
  , { status, headers, body } = await fetch (URI, options)
  , type = headers.get ('content-type')

//t.notOk
console.log ('type', headers.get ('content-type'))
console.log ('status', status)
//t.ok (type.match (/application\/javascript/))
//t.ok (type.match (/charset=utf-8/))
//t.equal (body, dist)

  server.close ``
  t.end ()
})
