const
  { test, serve, read, fetch }
    = require ('test')


test ('calling next middlewaree')


test.only ('GET / defaults to dist/snuggsi.min.js', async t => {

  const
    server = serve ``
  , dist   = read `dist/snuggsi.min.js`

  t.end ()
})
